export type DraftCommand = {
    alias?: string;
    command: string;
    description: string;
};

export type Command = {
    id: string;
    alias?: string;
    command: string;
    description: string;
};
